#pragma once
/*#include <utility>
#include "Player.h"
#include "Ball.h"
#include "Game.h"

//class Game;

class GameEvents {
public:
	GameEvents();
	~GameEvents();

	virtual void Collisions(Game *g);
	virtual bool CheckCollision(Ball* b, Player* p);
	virtual void IncrementScore(std::map<std::string, Player*> _players, Player* player);


private:
};*/
